export const recognizeJobCard = async (
  imageUri: string
): Promise<string> => {

  console.log("Image for OCR:", imageUri);

  /*
    Later connect:

    - Google ML Kit
    - Tesseract
    - Custom backend OCR
    - Offline OCR
  */

  return "";
};